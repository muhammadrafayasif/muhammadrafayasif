name: Metrics

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches: ["main", "master"]

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.API_KEY }}

          base: header, activity, repositories, metadata

          config_display: large
          config_padding: 0
          config_timezone: Asia/Karachi

          plugin_notable: yes
          plugin_notable_filter: stars>2
          plugin_notable_indepth: yes
          plugin_notable_repositories: yes
          plugin_notable_from: all

          plugin_activity: yes
          plugin_activity_limit: 5
          plugin_activity_days: 7

          plugin_repositories: yes
          plugin_repositories_featured: yes
          plugin_repositories_featured_count: 4

          commits_authoring: muhammadrafayasif, muhammadrafay3561@gmail.com, rafay4730069@cloud.neduet.edu.pk

          plugin_languages: no
          plugin_achievements: no
          plugin_topics: no

          # 📄 Output
          filename: metrics.svg
